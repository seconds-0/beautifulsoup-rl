# BeautifulSoup RL Training Environment Variables
# Copy to .env and fill in your credentials
# NEVER commit the actual .env file!

# =============================================================================
# Backblaze B2 (Checkpoint Storage)
# =============================================================================
# Create a bucket-scoped application key (not master key) with these permissions:
# - listFiles, readFiles, writeFiles, deleteFiles
# - Bucket: beautifulsoup-rl only
# - No key management or bucket admin permissions
B2_APPLICATION_KEY_ID=your_bucket_scoped_key_id
B2_APPLICATION_KEY=your_bucket_scoped_app_key
B2_BUCKET=beautifulsoup-rl

# =============================================================================
# WandB (Experiment Tracking)
# =============================================================================
# Get from: https://wandb.ai/authorize
WANDB_API_KEY=your_wandb_api_key

# For resuming a specific run (set by checkpoint_sync.sh, or manually for resume)
# WANDB_RUN_ID=optional_run_id_for_resume

# =============================================================================
# Vast.ai (GPU Provisioning)
# =============================================================================
# Get from: https://cloud.vast.ai/api/
VAST_API_KEY=your_vast_api_key

# =============================================================================
# Training Configuration
# =============================================================================
# Unique identifier for this training run (used for checkpoint paths)
RUN_ID=bs4-qwen3-8b

# =============================================================================
# vLLM Configuration (Set automatically by pod_setup.sh)
# =============================================================================
# These prevent CUDA segfaults and multiprocessing issues
VLLM_USE_V1=0
VLLM_WORKER_MULTIPROC_METHOD=spawn
