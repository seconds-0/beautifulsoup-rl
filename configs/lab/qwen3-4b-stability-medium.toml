# Qwen3-4B Stability Test: Medium
#
# Hypothesis: Fewer rollouts = more stable
# Strategy: 4x reduction in rollouts per step (2048 → 512)
#
# Changes from production:
# - rollouts_per_example: 8 → 4
# - oversampling_factor: 2.0 → 1.0
# - batch_size unchanged at 128

model = "Qwen/Qwen3-4B-Instruct-2507"
max_steps = 100
batch_size = 128
rollouts_per_example = 4
trajectory_strategy = "interleaved"
max_async_level = 2
oversampling_factor = 1.0

[sampling]
max_tokens = 4096

[[env]]
id = "seconds-0/beautiful-soup-env"
args = { split = "train", mode = "bootstrap", cpu_cores = 2, memory_gb = 8 }

[env.log]
level = "info"
vf_level = "info"
